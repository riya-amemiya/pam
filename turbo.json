{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "outputs": [
                ".next/**",
                "!.next/cache/**"
            ],
            "inputs": [
                "src/**"
            ]
        },
        "lint": {}
    },
    "globalDependencies": [
        "tsconfig.json"
    ]
}